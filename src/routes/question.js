// const express = require('express')
// const router = express.Router()

// const passport = require('passport')

// import { getAllByCategory, getQuestion, voteUp, voteDown, publish } from 'controllers/question'

// router.post('/publish', passport.authenticate('jwt', {
//   session: false,
// }), publish)

// router.post('/voteUp', passport.authenticate('jwt', {
//   session: false,
// }), voteUp)

// router.get('/:questionId', passport.authenticate('jwt', {
//   session: false,
// }), getQuestion)

// router.get('/category/:category/:pageNumber', getAllByCategory)

// router.get('/filter/all/:pageNumber', getAll)

module.exports = router